<div class="col-md-12 col-md-offset-4">
  <div class="group">
    <%= link_to("edit", edit_group_path(@group), class: "btn btn-primary pull-right") %>
  </div>
  <h2><%= @group.title %></td></h2>
  <p><%= @group.description %></p>
</div>
